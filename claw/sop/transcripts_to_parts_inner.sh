# 02_transcripts to 03_parts
#
# Called by 

process () {
echo "
# Goal（一句话）
'${1}'是一场4小时(一般为4小时)的直播连麦音频字幕，连麦内容一般包括：
- 开场白
- 第一个嘉宾上线，提问，主播回答疑惑，多轮对话。一次连麦大约20分钟左右。
- 嘉宾下线后，主播加以评论，可能会引申。
- 第二个嘉宾...重复上述环节
请先理解语料，回答一共有多少次场连麦，给出连麦开始和结束的时间戳。
然后根据时间戳，按照00_开场 01_<嘉宾人设>_连麦 02_<嘉宾人设>_评论 03_...
划分得到多个子文件。

然后对每个子文件，识别说话人（连麦只有2个人，评论只有1个人），对字幕划分句子，并保留每个句子对应的时间戳，更新这个文件。

# Output（必须写入指定目录）
- 输出路径为'${1}'去除.json后缀的同名文件夹（不存在则创建）
- 子文件命名：00_开场 01_<嘉宾人设>_连麦 02_<嘉宾人设>_评论 ...
- 每个子文件保存为 JSON，路径为 <OUT_DIR>/<分段文件名>.json，且符合 data/03_transcripts/formal_output.schema.json。
- 仅使用 Write/Edit 工具写入，禁止写到其它路径。

# Non-goals（明确不做什么）
1. 不要尝试通过写脚本来过滤。必须先自己先把完整字幕看一遍。如果觉得一次看完太多，可以把字幕分成10份，一次看1份，然后拼起来
2. 不准通过写python脚本来实现文本内容的解析，必须通过自己的理解来解析，自己的理解的意思是，调用${2}设置合理的prompt让AI输出解析结果
2. 不准通过写python脚本来实现文本内容的解析，必须通过自己的理解来解析，自己的理解的意思是，调用${2}设置合理的prompt让AI输出解析结果

# Notes
1. 使用${2}来执行文本处理任务，设置好输出格式
2. 保存的json符合data/03_transcripts/formal_output.schema.json格式

# Steps（必须先给 plan）
1. 理解整个字幕，给一个大概的估计，划分多少段。
0. 先判断当前json文件是否已经处理了。如果有处理的文件，查看处理的文件划分，是否和你第一步认为的一致。
    如果一致，就跳过后续步骤，直接回复 当前已处理，完成
    如果不一致，可能之前的处理中断了，则补全遗漏的，继续后续步骤。
2. 对于每一段，调用${2}完成文本分析任务，自行设计prompt，保存处理的结果，这一步可以并行，最大可以4个${2}同时操作
3. 处理整合结果，特别是每一段边界处，避免连续说话被分两段
4. 检查人物id识别结果，自行编写python脚本调用一些音频特征分析工具，分析这段时间戳对应的音频的频谱，修改识别错误的case。
5. 检查是否符合data/03_transcripts/formal_output.schema.json，不符合的地方直接fix

# Done（可自动验证）
1. 得到 <OUT_DIR> 处理后的文件夹，文件名按照00_开场 01_<嘉宾人设>_连麦 02_<嘉宾人设>_评论，每一段文件里面，包含：
  - 正确的人物id
  - 正确的分句，每一句有对应的时间戳（便于后续作为播放器的字幕显示）
  - 无语气词、不连续、被截断等等问题。
2. 完成音频特征分析检查
3. 生成的json格式，符合data/03_transcripts/formal_output.schema.json
"
}

default_jsonfile="data/02_transcripts/曲曲2025（全）/01 - 睡美人2025年第一場直播 臥播主打就是鬆弛感 2025年1月2日 ｜ 曲曲麥肯錫.json"
default_ai="codex exec"

process "${JSON_FILE:-$default_jsonfile}" "${AI:-$default_ai}"
