给定训练任务书，在h20上完成训练任务。

# Inputs（路径 / repo / 版本）
1. h20: ssh -p 39670 root@115.190.130.100
2. 在h20上完成训练sft任务。代码、数据都在h20上

# Non-goals（明确不做什么）
1. 不修改任何本地的文件，所有操作必须且只能通过ssh到h20上完成。
2. 不能修改h20其他非项目路径的文件，只有persona下的文件文件夹具备读写权限
3. 不要直接在当前session进行训练任务，必须用tmux session来启动训练任务，以及所有耗时的长时间操作。

# Steps（必须先给 plan）
1. 理解任务书，制订计划。
2. 实施计划。
3. 启动sub-agent验收结果，给出验收报告。
4. 如果不满足，分析问题，找到解决方案，重复步骤2/3，知道验收通过。

# Done（可自动验证）
完成任务书的完成标准。
